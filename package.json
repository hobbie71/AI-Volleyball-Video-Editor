{
  "name": "volleyball-video-editor",
  "private": true,
  "scripts": {
    "client": "npm start --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "@types/mime-types": "^3.0.1",
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "mime-types": "^3.0.1",
    "react-icons": "^5.5.0",
    "tsc": "^2.0.4",
    "uuid": "^11.1.0"
  }
}
