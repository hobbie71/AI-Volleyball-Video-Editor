// Client-specific video types that depend on browser APIs
export type VideoElementObject = Record<string, HTMLVideoElement>;

// Re-export shared types for convenience
export * from '@shared/types/video.types';
